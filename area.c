#include <stdio.h>
int main ()
{
    int a,b;
    //asking the user for input of the breadth and length
    printf("enter the length of the rectangle a\n");
    scanf("%d",&a);
    printf ("enter the breadth of the rectangle b\n");
    scanf("%d",&b);
    printf ("the area of the rectangle = %d",a*b);
    return 0;

}